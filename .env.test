# Test-Environment-Variablen — gelten ausschließlich beim Vitest-Lauf
# (`vitest` lädt .env.test automatisch). Demo-Modus aktivieren, damit
# UserProvider eine synchrone Demo-Session liefert (siehe ArbeitsplatzPage.test.tsx).
VITE_DEMO_MODE=1

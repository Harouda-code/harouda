# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test + Build caches
coverage
.vitest-cache
*.tsbuildinfo

# Supabase CLI workspaces (local only)
supabase/.branches
supabase/.temp

# Windows
Thumbs.db

# Generated from vite.config.ts (build artefacts, don't commit)
vite.config.d.ts
vite.config.js

# Claude Code workspace config (workspace-local, personal)
.claude/

# env — only the example template is committed
.env
.env.*
!.env.example

# Test-Config (kein Secret — VITE_DEMO_MODE für Vitest)
!.env.test

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# === Sprint 2.0: Secrets hardening (never commit) ===
# Private keys / certificates — eIDAS, JWT RS256, ELSTER, DEUeV
*.key
*.pem
*.pfx
*.p12
# future: ERiC Java-Interop (not used yet)
*.jks

# Credential files (cloud service accounts, OAuth client secrets)
credentials.json
*.secret
.env.vault

# === Sprint 2.0: Additional tool caches / artefacts ===
# Supabase CLI project-link state (different from supabase/.branches|.temp above)
.supabase/

# Alternative bundler outputs (defense-in-depth)
build/
out/
.next/
.vite/

# ESLint cache
.eslintcache

# Windows folder customization
desktop.ini

# === Sprint 2.0: Editor/merge artefacts ===
*.bak
*.orig

# Temporary discovery/handoff dumps
_temp-*.txt
